const functions = require("firebase-functions");
const admin = require("firebase-admin");

// Inicjalizacja środowiska administracyjnego Firebase
admin.initializeApp();

// Tutaj za chwilę dodamy funkcje wysyłające powiadomienia Push!
